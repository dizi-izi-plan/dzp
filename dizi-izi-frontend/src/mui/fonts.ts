import { Cabin, Manrope } from 'next/font/google';

export const manrope = Manrope({
  weight: ['300', '400'],
  subsets: ['latin'],
  display: 'swap',
});

export const cabin = Cabin({
  weight: ['400', '500', '600'],
  subsets: ['latin'],
  display: 'swap',
});
